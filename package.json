{
  "name": "chess-console-stockfish",
  "version": "6.2.4",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "bootstrap": "^5.3.8",
    "chess-console": "^6.13.3",
    "cm-chessboard": "^8.12.11",
    "cm-engine-runner": "^2.0.1",
    "cm-web-modules": "^2.7.0"
  },
  "description": "StockfishPlayer for ChessConsole",
  "scripts": {
    "dev": "bun build ./index.html --outdir ./dist --watch & bun run copy-assets --watch & bun server.js",
    "start": "bun run dev",
    "build": "rm -rf dist && bun build ./index.html --outdir ./dist --minify && bun run copy-assets",
    "copy-assets": "mkdir -p dist/assets && cp -r assets/* dist/assets/ 2>/dev/null || true && mkdir -p dist/assets/cm-chessboard && cp -r node_modules/cm-chessboard/assets/* dist/assets/cm-chessboard/ 2>/dev/null || true && cp -r public/* dist/ 2>/dev/null || true",
    "preview": "bun server.js",
    "serve": "bun run preview",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "typecheck": "echo 'No TypeScript configured; skipping typecheck'"
  },
  "type": "module"
}